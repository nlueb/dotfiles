
paths:
  home: "{{ '~' | expanduser }}"
  dotfiles: "{{ '~' | expanduser }}/dotfiles"
  config: "{{ '~' | expanduser }}/.config"

packages:
  arch:
    core:
      - xorg-server
      - xorg-xinit
      - xclip
      - bspwm
      - sxhkd
      - exa
      - zathura
      - zathura-pdf-poppler
      - firefox
      - dmenu
      - openssl
      - openssh
      # - texlive-most
      # - texlive-lang
      # - biber
    aur:
      - nerd-fonts-iosevka
      - bitwarden-bin
  # macos:
  global:
    - vim
    - neovim
    - tmux


# vim: set ft=yaml.ansible
