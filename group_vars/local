
paths:
  home: "{{ '~' | expanduser }}"
  dotfiles: "{{ '~' | expanduser }}/dotfiles"
  config: "{{ '~' | expanduser }}/.config"
  bin: "{{ '~' | expanduser }}/bin"

packages:
  arch:
    core:
      - xorg-server
      - xorg-xinit
      - xorg-xrandr
      - xclip
      - bspwm
      - sxhkd
      - exa # ls alternativ
      - bat # cat alternativ
      - zathura
      - zathura-pdf-poppler
      - firefox
      - dmenu
      - openssl
      - openssh
      - dunst
      # - texlive-most
      # - texlive-lang
      # - biber
    aur:
      - nerd-fonts-iosevka
      - bitwarden-bin
      - neovim-nightly
  # macos:
  global:
    - vim
    - neovim
    - tmux
    - nodejs
    - yarn


# vim: set ft=yaml.ansible
